[package]
name = "rsleigh-gen-aarch64-shared"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "rsleigh generated decoder for aarch64 — shared types (internal, do not depend on directly; use rsleigh-api)"
include = ["src/**", "out/**", "Cargo.toml"]

[dependencies]
pcode-ir = { path = "../../pcode-ir", version = "0.2.0" }

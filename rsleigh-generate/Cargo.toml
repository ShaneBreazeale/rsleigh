[package]
name = "rsleigh-generate"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "rsleigh codegen CLI — parses Ghidra .slaspec files and emits Rust decoder crates"
keywords = ["sleigh", "codegen", "disassembler", "ghidra", "parser-generator"]
categories = ["development-tools", "command-line-utilities"]

[dependencies]
rsleigh = { path = "..", version = "0.4.3", default-features = false }
